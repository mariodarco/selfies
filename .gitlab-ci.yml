image: "ruby:2.5.1"

# Cache gems in between builds
cache:
  paths:
    - vendor/ruby

before_script:
  - ruby -v
  - gem install bundler --no-ri --no-rdoc
  - bundle install -j $(nproc) --path vendor

rspec:
  script:
    - rspec spec

rubocop:
  script:
    - bundle exec rubocop